/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "res.cloudinary.com",
      "images.unsplash.com",
      "picsum.photos",
      "dummyimage.com"
    ]
  }
};

export default nextConfig;
